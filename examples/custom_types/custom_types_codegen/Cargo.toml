# This file was generated with `clorinde`. Do not modify.
[package]
name = "custom_types_codegen"
version = "0.1.0"
edition = "2021"
publish = false

[features]
default = ["deadpool", "chrono"]
deadpool = ["dep:deadpool-postgres", "tokio-postgres/default"]
wasm-async = ["tokio-postgres/js", "chrono?/wasmbind", "time?/wasm-bindgen"]

chrono = ["dep:chrono"]
time = ["dep:time"]

[dependencies]
## Core dependencies
# Postgres types
postgres-types = { workspace = true, features = ["derive"] }
# Postgres interaction
postgres-protocol = { workspace = true }
# Iterator utils required for working with `postgres_protocol::types::ArrayValues`
fallible-iterator = { workspace = true }

## Custom type crates
ctypes = { path = "../ctypes" }
postgres_range = { version = "0.11.1", features = ["with-chrono-0_4"] }

## Types dependencies
# TIME, DATE, TIMESTAMP or TIMESTAMPZ
chrono = { workspace = true, optional = true }
time = { workspace = true, optional = true }

## Async client dependencies
# Postgres async client
tokio-postgres = { workspace = true, features = ["with-chrono-0_4", "with-time-0_3"], default-features = false }
# Async utils
futures = { workspace = true }

## Async features dependencies
# Async connection pooling
deadpool-postgres = { workspace = true, optional = true }
